{
  "routes": [
    {
      "method": "GET",
      "path": "/music-page",
      "handler": "music-page.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/music-page",
      "handler": "music-page.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/music-page",
      "handler": "music-page.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
